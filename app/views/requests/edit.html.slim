= content_for :breadcrumbs, [[t("admin.br.request"), requests_path], [@request.user_name, requests_path(@user)], t("admin.br.edit")]
- provide :btn_text, t("admin.request.view.btn_edit")
= render "form"
